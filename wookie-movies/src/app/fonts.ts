import {Dorsa, Monoton} from 'next/font/google'

export const dorsa = Dorsa({
    subsets: ['latin'],
    display: 'swap',
    weight: '400'
})

export const monoton = Monoton({
    subsets: ['latin'],
    display: 'swap',
    weight: '400'
})